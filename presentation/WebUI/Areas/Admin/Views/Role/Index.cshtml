@model List<RoleViewModel>

<!-- Container-fluid starts-->
<div class="page-body">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="card card-table">
                    <!-- Table Start -->
                    <div class="card-body">
                        <div class="title-header option-title">
                            <h5>Icazələr Siyahısı</h5>
                        </div>
                        <div>
                            <div class="table-responsive">
                                <table id="table_id" class="table role-table all-package theme-table">
                                    <thead>
                                        <tr>
                                            <th>No</th>
                                            <th>İcazə Adı</th>
                                            <th>İmtiyaz</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                    @for(int i=0; i < Model.Count; i++)
                                {
                                    <tr>
                                            <td>@i</td>
                                            <td>@Model[i].Name</td>
                                            <td>role.Description</td>
                                        </tr>
                                }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <!-- Table End -->
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->
    <!-- footer start-->
    <div class="container-fluid">
        <footer class="footer">
            <div class="row">
                <div class="col-md-12 footer-copyright text-center">
                    <p class="mb-0">Copyright 2023 © SaytAdi theme by Lorem</p>
                </div>
            </div>
        </footer>
    </div>
</div>
<!-- Page Body End -->

<script>

    var editHandler = (e) => {
        $.post(`/admin/role/editrole`, { id: e.id, name: e.Name }, (data) => { alert(data })
    }

    var deleteHandler = (e) => {
        $.post(`/admin/role/deleterole/id=${e.id}`, (data) => { alert(data })
    }


</script>
